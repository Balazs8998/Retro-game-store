server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/retrogamedb?createDatabaseIfNotExist=true&serverTimezone=UTC&useSSL=false
    username: root
    password: test1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: tru
        format_sql: tru
        dialect: org.hibernate.dialec.MySQL8Dialect